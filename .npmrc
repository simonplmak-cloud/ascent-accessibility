# Hoist ESLint plugins to the root so eslint-config-next (flat config) can resolve
# them (eslint-plugin-react-hooks, eslint-plugin-jsx-a11y) under pnpm's strict layout.
public-hoist-pattern[]=*eslint*
